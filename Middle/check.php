<?php
  /*$url = "https://afsaccess3.njit.edu/~mjv43/CS490/Controller_Project/login_info.php";
  $ucid = 'jnwf';
  $password = 'pasad';
  $sess = array(
                "ucid" => $ucid,
                "password" => $password
            );
  $curl = curl_init($url);
  curl_setopt($curl, CURLOPT_RETURNTRANSFER, 1); 
  curl_setopt($curl, CURLOPT_POST, 1);
  curl_setopt($curl, CURLOPT_POSTFIELDS, $sess);
  $result = curl_exec($curl);
  curl_close($curl);
  echo $result;*/
?>